// Arrays #3

let latamCountries = [
  ['Argentina', 'Colombia', 'Uruguay', 'México'],
  ['Chile', 'Perú', 'Paraguay', 'Venezuela'],
  ['Ecuador', 'Bolivia', 'El Salvador', 'Guatemala'],
]

for (let r = 0; r < latamCountries.length; r++) {
  for (let c = 0; c < latamCountries[r].length; c++) {
    console.log(`- ${latamCountries[r][c]}`)
  }
}



